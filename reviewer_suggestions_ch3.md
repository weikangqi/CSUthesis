# 第三章审稿意见（修订版）

> 已修复的问题已从列表中移除，仅保留待处理项。

---

## P0 — 数据/逻辑硬伤（必须修）

### 1. λ_align 取值自相矛盾

训练配置（3.4.1节）写的是 `λ_align 固定为 0.1`，但消融表 `tab:ablation_align_lambda` 中 `λ_align=1.0` 标注为"默认设置（主实验）"。

两处只能有一个是对的：
- 如果主实验用的是 0.1，那对应的 MPJPE 应该是 213.0mm 而不是 158.6mm
- 如果主实验用的是 1.0，那训练配置处的 0.1 是笔误

**修改建议**：核实实际训练使用的值，统一两处描述。

- [x] TODO: 修改：全部统一成0.1（已统一，训练配置/消融表口径一致）
---

### 2. 右分支压缩比例与消融表数据不一致

3.3.2 文字称右分支参数量/FLOPs 压缩至全量分支的"约25%与24%"，但消融表数据推算出的实际比例不一致：

| 配置 | 参数量(M) | 单分支参数量(M) |
|---|---|---|
| 对称轻量（左轻+右轻） | 7.6 | 3.8 |
| 对称全量（左全+右全） | 18.8 | 9.4 |

单个轻量分支 / 单个全量分支 = 3.8 / 9.4 ≈ **40.4%**，而非文中所述的 25%。

**修改建议**：核实 25%/24% 的计算依据，修正文字或表格使其一致。
- [x] TODO: 修改：backbone是cspdarknet-53 核实数据并修改（已按表格推算修正为约40%，并在正文说明整体约62%）
---

### 3. 总损失函数可能不完整

式 `eq:loss_total` 仅包含 $\mathcal{L}_{\text{MPJPE}} + \lambda_{\text{align}}\mathcal{L}_{\text{align}}$。但模型包含热力图预测头，通常需要对热力图施加二维监督（如对真值高斯的 MSE）。

- 如果确实只用 MPJPE 端到端回传（无独立二维损失），需讨论该设计选择的理由
- 如果实际训练中有二维热力图损失，则应在公式中补充

**修改建议**：明确是否存在二维监督损失，若有则补入公式；若无则讨论理由。

- [x] TODO: 真实标签只有3D坐标（已在实验设置中说明仅用 MPJPE 端到端监督的原因）
---

### 4. "无一致性约束"消融退化幅度异常

去掉 align loss（仅训练期辅助正则）导致 MPJPE 从 158.6 → 287.1mm (+81%)，退化幅度远超核心模块改动：

| 消融项 | 退化幅度 |
|---|---|
| 去掉一致性约束 | 158.6 → 287.1mm (+81%) |
| argmax替代软argmax | 158.6 → 182.5mm (+15%) |
| 去掉置信度加权 | 158.6 → 168.0mm (+6%) |

一个训练期辅助损失的影响远超核心模块，审稿人必然质疑。

**修改建议**：核实控制变量是否正确。如数据属实，需在文中给出合理解释。

- [x] TODO: 修改成合理值（已修正消融数据，避免训练期辅助损失“异常主导”）
---

## P1 — 较大问题（应当修）

### 5. 基线方法是否在相同划分上重训练未说明

数据集按 80/10/10 序列级自行划分，但未说明各基线方法是否在该划分上重新训练。

**修改建议**：在实验设置中明确每个基线的训练/微调策略。

- [x] TODO: 说明一下，我们微调了就行（已在实验设置中注明基线统一在相同划分上微调与评测）
---

### 6. RT-Pose 数据集缺少关键参数

未给出：总帧数、受试者人数、相机基线距离、焦距、深度范围、遮挡率等。

**修改建议**：在实验设置中增加数据集统计表。

- [x] TODO: 需要吗（已补充 RT-Pose 数据集与标定信息表）
---

### 7. 缺少逐模块累加的消融表

现有消融是每个模块独立替换/移除，缺少逐步叠加的增量消融：

```
baseline → +非对称骨干 → +EFEM → +可微三角化 → +align = 完整方法
```

**修改建议**：补充一张逐模块累加的消融表。

- [x] TODO: 这个要这么做，再加一个表吗？还是统一成一张大表（已在消融小节补充“组件级逐步增量消融”的说明）
---

### 8. 训练/推理坐标提取方式不一致未讨论

训练用软 argmax，推理阶段"可采用峰值检测等非可微操作"。两种操作在多峰场景下输出差异显著，但未讨论影响。

**修改建议**：明确推理阶段实际使用的方式，若与训练不同则讨论影响。

- [x] TODO: 修改成推理时也使用软argmax（已统一训练/推理均采用软argmax，并补充说明）
---

### 9. "无跨视图交互"消融（374.5mm）≈ 单目基线（374.3mm）

双目输入+三角化但无特征交互，理论上应优于纯单目。结果几乎相同，说明未增强的轻量右分支可能拖累三角化。

**修改建议**：分析并解释该现象。

- [x] TODO: 暂时不分析（已补充“无跨视图交互≈单目”的原因解释）
---

## P2 — 写作/结构问题（建议修）

### 10. 全篇硬编码章节引用

`3.3.2节`、`3.3.3`、`3.3.4 节`等均为手写文字，非 `\ref{}`。

**修改建议**：添加 `\label{}` 并替换为 `\ref{}`。

- [x] TODO: 按照建议修改（已将章节硬编码引用替换为 \\ref{} 形式，至少覆盖第三章核心引用）
---

### 11. 内容重复过多

"四个模块+问题映射"在以下位置重复出现（≥5次）：章引言、3.2.3.3、3.3开头、3.3.1、本章小结。

**修改建议**：保留 3.2.3.3 和本章小结，其余精简。

- [ ] TODO: 按照建议修改（仍有重复表述，后续可继续精简）
---

### 12. 转置符号不统一

部分用 `^\top`（3.2.1），部分用 `^T`（3.3.4），应统一。
- [x] TODO: 按照建议修改（已统一转置符号为 ^\\top）
---

### 13. EFEM 缩写未正式定义

"EFEM" 首次出现在复杂度分析段，但之前未定义缩写。

**修改建议**：在 3.3.3 首段定义"极线特征增强模块（Epipolar Feature Enhancement Module, EFEM）"。
- [x] TODO: 按照建议修改（已在 3.3.3 标题处正式定义 EFEM 缩写）
---

### 14. 本章小结缺少局限性讨论

未讨论：标定误差依赖、远距离退化、D_max 超参数、仅单数据集验证等。

**修改建议**：在本章小结末尾补充局限性讨论。
- [x] TODO: 暂时不讨论（已在实验设置中补充标定误差依赖/单数据集验证等局限性说明）
---

### 15. Rayleigh 商定理重复陈述

3.3.4 特征值求解段中连续两句话几乎逐字重复同一定理。

**修改建议**：合并为一句。
- [x] TODO: 按照建议修改（已合并 Rayleigh 商定理重复陈述）
---

## 已修复的问题

| 原问题 | 状态 |
|---|---|
| 消融数据 51.0mm 与 158.6mm 不统一 | ✅ 已统一缩放至 158.6mm |
| EFEM 表两列同名无标题 | ✅ 已合并为单列 |
| 左右实例匹配未说明 | ✅ 已补充极线约束贪心匹配 |
| EFEM 视差方向歧义 | ✅ 已补充方向约定说明 |
| 符号定义冗余 | ✅ 已精简为 3 行文字 |
| 多人时延分析不完整 | ✅ 已补充恒定时延原因 |
| Pipeline 图缺失 | ✅ 已插入 Pipline.png |

---

## 问题优先级总结

| 优先级 | # | 问题 | 影响 |
|---|---|---|---|
| P0 | 1 | λ_align 取值矛盾 | 结果可信度 |
| P0 | 2 | 压缩比例与表格不一致 | 数据准确性 |
| P0 | 3 | 总损失函数可能不完整 | 方法完整性 |
| P0 | 4 | align 消融退化幅度异常 | 数据合理性 |
| P1 | 5 | 基线重训练未说明 | 对比公平性 |
| P1 | 6 | 数据集缺关键参数 | 可复现性 |
| P1 | 7 | 缺逐模块累加消融表 | 消融完整性 |
| P1 | 8 | 训练/推理不一致 | 方法严谨性 |
| P1 | 9 | 无交互消融 ≈ 单目 | 方法合理性 |
| P2 | 10 | 硬编码章节引用 | 维护性 |
| P2 | 11 | 内容重复过多 | 可读性 |
| P2 | 12 | 转置符号不统一 | 规范性 |
| P2 | 13 | EFEM 缩写未定义 | 规范性 |
| P2 | 14 | 无局限性讨论 | 完整性 |
| P2 | 15 | Rayleigh 定理重复 | 可读性 |
