# 第三章审稿意见

## 一、重大问题（必须解决）

### 1. 消融实验数据来源混乱——可信度核心问题

这是最严重的问题。主实验（Tab. rtpose_benchmark）本文方法 MPJPE = **158.6mm**，但多个消融表中"本文方法"为 **51.0mm**：

| 表格 | "本文方法" MPJPE |
|---|---|
| tab:ablation_efem（第二列） | 158.6mm ✓ |
| tab:ablation_efem（第一列） | 51.0mm ✗ |
| tab:ablation_solver | 51.0mm ✗ |
| tab:ablation_align | 51.0mm ✗ |
| tab:ablation_align_lambda（λ=1.0） | 51.0mm ✗ |
| tab:ablation_solver_components（最终行） | 158.6mm ✓ |

51.0mm 和 158.6mm 相差3倍以上，显然来自**两个不同数据集**。审稿人必然追问：

- 消融实验到底在哪个数据集上做的？
- 如果在"公开基准数据集"上，为何主实验在 RT-Pose 而消融实验在另一个数据集上？
- 两个数据集难度差异如此巨大，设计选择在一个数据集上有效是否能迁移到另一个？

**修改建议**：在所有消融实验中明确标注数据集来源。理想情况下，所有消融实验应在主实验数据集（RT-Pose）上重新进行。

---

### 2. EFEM 消融表（tab:ablation_efem）两列 MPJPE 没有列标题

```
特征增强模块 | MPJPE(mm)↓ | MPJPE(mm)↓
```

两列同名，读者完全无法分辨分别代表什么。结合问题1，这两列很可能分别是公开基准和 RT-Pose 的结果，但完全没有说明。

**修改建议**：为两列分别添加明确的标题（如"MPJPE(mm)↓（公开基准）"和"MPJPE(mm)↓（RT-Pose）"），并在表格说明中注明数据来源。

---

### 3. 左右视图实例匹配问题未说明

3.3.1节描述了"中心点引导的自底向上解码"，从左右视图分别检测实例，但**完全没有说明如何将左视图检测到的第 $m$ 个人与右视图检测到的第 $m'$ 个人进行对应**。这是双目多人姿态估计的核心工程问题：

- 左右视图检测到的人数不一定相同
- 实例顺序也不一定对应
- 错误匹配会直接导致三角化失败

若没有解决此问题，整个框架在多人场景下的正确性存疑。

**修改建议**：补充跨视图实例关联策略（如基于中心点极线距离的匹配、Hungarian 匹配等）的说明。

---

## 二、较大问题（应当解决）

### 4. 只在一个非标准数据集上验证

所有实验仅在 RT-Pose 数据集上进行，缺乏对 Human3.6M、MPI-INF-3DHP 等标准基准的验证，泛化性未知。

**修改建议**：若确实无法在标准数据集上测试（如缺少双目数据），应在论文中明确说明原因，并定量描述 RT-Pose 与标准数据集的难度关系（如场景复杂度、遮挡率、人数分布等）。

---

### 5. 跨分支一致性约束与知识蒸馏的区别未说明

3.3.5节的设计（全量分支教轻量分支、stop-gradient、逐层特征对齐）与**特征级知识蒸馏（Feature Distillation）**几乎完全相同，但文中完全没有引用知识蒸馏相关文献，也没有说明与已有方法的区别。

**修改建议**：在相关工作部分补充知识蒸馏相关引用，并在方法部分明确说明本文一致性约束与传统蒸馏的联系与区别（如是否使用教师网络独立训练、是否在线蒸馏等）。

---

### 6. EFEM 视差方向的歧义

公式中：

$$s(u,v,d) = \frac{\langle \mathbf{q}(u,v), \mathbf{k}(u+d,v) \rangle}{\sqrt{C'}}$$

右视图查询在 $(u,v)$，搜索左视图键在 $(u+d, v)$，即 $d \geq 0$ 时左视图特征在右视图特征的**右侧**。但立体校正后的标准约定是左视图对应点在右视图对应点的右侧，即视差为左减右（$d = u_l - u_r \geq 0$）。若以右分支特征 $(u_r, v)$ 为锚点搜索左分支特征，应为 $(u_r + d, v)$，此时 $d > 0$ 符合约定。但需要确认：在特征图坐标下视差方向是否与图像坐标一致？

**修改建议**：在公式旁明确说明视差方向约定（$d$ 为正值，表示左图特征相对右图特征向右偏移），并说明与图像极线几何的对应关系。

---

### 7. 比较基线的代表性不足

- RTMW3D（92.3M参数，136.56G FLOPs）本身不是端侧轻量化模型，作为端侧对比失去公平性
- 缺乏与近年轻量化方法（如 MotionBERT-Lite、PoseFormerV2 等）的对比
- 论文承认"缺少端到端双目三维姿态工作可直接对比"，但未讨论与 StereoMocap 等双目方法的关系

**修改建议**：补充说明选取各基线的理由，并对精度差较大的对比进行更深入的原因分析（如数据集与方法设计的匹配度）。

---

## 三、一般性建议

### 8. 标定误差敏感性未讨论

EFEM 强依赖极线校正精度，可微三角化强依赖相机投影矩阵精度。实际部署中标定误差不可避免，文中没有分析标定误差对最终 MPJPE 的影响，也没有给出 RT-Pose 数据集的基线距离等关键参数。

**修改建议**：补充标定误差鲁棒性分析（哪怕是定性描述），并在实验设置中注明 RT-Pose 的相机基线距离。

---

### 9. 3.3.4 节符号定义冗余

3.3.4节的 itemize 符号定义列表中大部分符号在章节前已出现，重复定义增加篇幅但价值有限；同时该节整体长度（约200行）明显高于其他节。

**修改建议**：将符号定义缩减为2~3行文字说明，其余符号在正文中首次出现时就地定义。

---

### 10. 多人可扩展性分析中 RTMO+MobileStereoNet 时延不变的原因未说明

表中 RTMO+MobileStereoNet 在1人、5人、10人下延迟均为 189.2ms，完全不随人数变化。这虽然合理（深度估计阶段与人数无关），但没有解释，读者会困惑：若它也与人数无关，为何不如本文方法更具优势？

**修改建议**：在图/表说明中补充说明 RTMO+MobileStereoNet 时延不变的原因，并点明本文方法时延更低（116ms vs 189ms）的同时兼顾精度优势（158.6mm vs 202.0mm）。

---

## 问题优先级总结

| 优先级 | 问题 | 影响 |
|---|---|---|
| P0（必须修） | 消融实验数据集不统一 | 论文可信度 |
| P0（必须修） | EFEM表双列无标题 | 数据可读性 |
| P0（必须修） | 左右实例匹配未说明 | 方法完整性 |
| P1（应当修） | 仅单数据集验证 | 泛化性 |
| P1（应当修） | 缺知识蒸馏相关工作 | 创新性论证 |
| P1（应当修） | EFEM视差方向歧义 | 公式正确性 |
| P1（应当修） | 基线代表性不足 | 对比公平性 |
| P2（建议修） | 标定误差未讨论 | 实用性 |
| P2（建议修） | 符号定义冗余 | 可读性 |
| P2（建议修） | 多人延迟分析不完整 | 论证清晰度 |
